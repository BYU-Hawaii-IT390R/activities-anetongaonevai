Extra Credit Task: --top-commands
This extra task lists the most common shell commands used by bots in the Cowrie honeypot log file.
It works by searching each line of the log for 'CMD:' entries and counting how often each command appears.

# regex assisted by ChatGPT: prompt "regex to match 'CMD:' lines in Cowrie logs"
